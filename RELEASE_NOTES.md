# GPT TouchBar HUD v0.1.29

刘海融合改为从屏幕顶边连续绘制的完整黑色外壳，在菜单栏高度内只覆盖系统安全区给出的中央包围范围，文字、按钮和点击区域仍位于安全区下方。收起和展开使用同一份几何与动画进度，补齐 v0.1.28 在硬件刘海底角周边可能出现的背景缺口和横向台阶。按用户确认的视觉方案，展开轮廓采用短凹圆角、水平肩线和外圆角，长摘要会预留必要顶部空间。新增与正式几何、原生视图共用实现的独立调试入口，可用假刘海、不同背景和动画进度复现并生成截图；该入口不编入正式 App。

新增默认开启的自动检查更新。正式安装在 `/Applications` 或 `~/Applications` 后，应用启动约 30 秒再按需检查，成功后 24 小时内不重复请求，睡眠唤醒只补一次到期检查。后台无更新和失败时保持静默；发现新版本只在菜单和“设置 → 更新”显示入口，不抢焦点、不展开刘海，也不自动下载或安装。用户主动查看版本说明后，可以选择安装并重启、稍后或跳过当前版本；手动检查始终保留，并可再次找到已跳过版本。

更新请求会持久保存最近尝试、最近成功、可用版本、跳过版本和重试期限，失败采用有限退避并尊重 GitHub 限流。手动与后台检查共享同一次请求，下载安装期间不会重复检查。开发 worktree、SwiftPM/`swiftc` 测试和刘海模拟入口不会启动真实自动检查。

## 使用说明与验收边界

- 自动检查只获取本仓库最新正式 Release；草稿、预发布版、降级版本和不可信附件不会进入安装流程。
- 下载安装仍必须由用户主动确认，并沿用包大小、SHA-256、应用标识、版本、架构、最低系统版本和签名校验；更新完成后只重启额度工具，不退出 ChatGPT。
- 安装包按 arm64 和 x86_64 分别构建。本地候选仅验证 arm64；x86_64 需要发布 CI 补验。
- 实体刘海上的接缝、色差、圆角肩部、点击穿透、全屏、多屏和睡眠恢复仍需真机验收；原生 synthetic 模拟通过不等于硬件验收完成。
- 正式安装路径中的定时检查、真实 GitHub 网络/限流和更新重启链路尚未做端到端验收。
- 任务状态和默认关闭的 Hooks 实验沿用 v0.1.28 口径。本版没有实施任务问号状态改造，也不新增真实 Hooks 启用或准确性结论。
- 继续声明 macOS 11 最低目标，采用 ad-hoc 签名，尚未经过 Apple 公证。首次打开方法见安装包内说明。
