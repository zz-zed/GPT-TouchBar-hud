# GPT TouchBar HUD v0.1.26

本次更新重新整理菜单栏、桌面浮窗和 Touch Bar 的展示。没有 Touch Bar 的 Mac 同样可以通过菜单栏和浮窗查看额度、用量与任务状态。

## 新增与改进

- 桌面浮窗采用 Quiet 单行胶囊，宽度随任务和额度内容自动伸缩；移除关闭按钮，通过菜单隐藏，保留刷新操作。
- 菜单顶部集中展示额度、重置或到期时间、任务状态、昨日/累计 Token 和点数；下方保留常用操作。横向闪电图标继续随任务状态变色，支持重置卡外显。
- 新增独立设置窗口，分为通用、外观和 Touch Bar，提供实时浮窗预览。语言、任务提示、颜色和不透明度修改即时保存，保留已有偏好。
- Touch Bar 采用紧凑的 Balanced 布局，标题、数值和日期使用白色粗体，日期列对齐；昨日/累计上下排列，点数有值时追加显示，保留额度进度条。

## 使用说明

- 根据处理器选择 arm64 或 x86_64 安装包；菜单栏和桌面浮窗均无需 Touch Bar。
- 任务状态与语言设置移至“设置… → 通用”，浮窗外观移至“设置… → 外观”。
- 应用仍默认隐藏浮窗；从菜单栏选择“显示浮窗”即可打开。退出应用请使用菜单栏的“退出”。
- 当前采用 ad-hoc 签名，尚未经过 Apple 公证。首次打开方法见安装包内说明。
