# GPT TouchBar HUD v0.1.28

菜单栏按当前图标和额度文字收放宽度，减少空白占位。刘海面板采用更紧凑的常驻摘要，同时显示任务数量与额度；展开详情会随内容自然增高，长内容可滚动，展开与收起保持顶部和中心稳定。多屏选择、隐藏偏好和设置退出入口一并完善；无可用刘海区域时继续使用桌面浮窗。

新增默认关闭的原生 Hooks 任务监测实验，原有日志模式仍为默认。实验模式区分执行中、已提交待核对、未知、覆盖范围与连接状态，避免把未确认状态显示成零。新完成事件的提示显示约 4 秒，刷新或隐藏后重开不重复播放，同轮继续执行会撤销失效提示。Touch Bar 按完整任务数字和未知标记扩展角标宽度。

## 使用说明

- 在“设置 → 实验功能”进入 Hooks 配置，先审阅变更计划，再决定是否应用。启用还需宿主正常信任流程；打开设置或升级应用不会自动启用实验。
- Hooks 的覆盖范围仍可能不完整，不承诺桌面全量任务数、精确空闲或真实宿主延迟；没有省电收益结论。
- 关闭 Hooks 会恢复原有日志监测。已写入的 Hook 定义与稳定 helper 需要通过独立清理流程处理，删除 App 不会自动清理它们。
- 根据处理器选择 arm64 或 x86_64 安装包；当前本地准备仅产出 arm64 包，Intel 包需后续 CI 验证后生成。
- 继续声明 macOS 11 最低目标；本地 arm64 编译链接目标已核对，macOS 11 实际运行尚未验收。
- 实体刘海接缝、透明角点击穿透、全屏/多屏/Space、锁屏唤醒与 Dock 启动行为，以及真实宿主 Hooks 执行仍待验收。
- 当前采用 ad-hoc 签名，尚未经过 Apple 公证。首次打开方法见安装包内说明。
