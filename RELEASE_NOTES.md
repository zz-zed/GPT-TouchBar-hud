# GPT TouchBar HUD v0.1.30

“设置 → 通用”新增明确的“刘海常驻形态”选择，替代原来的“刘海始终显示额度”复选框：

- **额度预览态 Peek（默认）**：未保存过该偏好时，静止状态也显示额度与重置时间。
- **静止态 Compact**：静止时只保留刘海两侧指示器，悬停后预览额度。

已有用户保存的选择继续保留：原复选框关闭对应 Compact，开启对应 Peek。两种常驻形态均可点击展开“额度 / 活动 / 用量”，收起后返回所选形态。显示模式和隐藏状态独立保留，不会因默认值变化强制显示面板。

README 同步更新刘海三态、设置、菜单栏自动规则、自动检查更新、Hooks 数据来源及本地存储说明，并补齐原生预览图片与验证命令。

## 使用与验证边界

- 本版为 0.1.30 / Build 33，分别提供 Apple Silicon（arm64）和 Intel（x86_64）安装包。
- 面板轮廓、动画和详情交互沿用 v0.1.29；任务状态与默认关闭的 Hooks 实验口径保持不变。
- 自动检查更新沿用 v0.1.29：后台只提示可用版本，下载安装仍须用户主动确认。
- 实体刘海接缝、全屏、多屏及睡眠恢复，实际 macOS 11 运行，以及正式安装路径中的自动更新完整链路仍需真机验收。
- 安装包采用 ad-hoc 签名，尚未经过 Apple 公证；首次打开方法见安装包内说明。
