import AppKit

/// Shared metrics for the confirmed Quiet / Status-first / Balanced design.
enum DesignTokens {
    static let hudHeight: CGFloat = 40
    static let hudInset: CGFloat = 12
    static let spacing: CGFloat = 6
    static let hudSpacing: CGFloat = 10
    static let buttonSize: CGFloat = 26
    static let touchBarHeight: CGFloat = 30
    static let progressWidth: CGFloat = 40
    static let accent = NSColor(calibratedRed: 0.44, green: 0.79, blue: 0.71, alpha: 1)
}
